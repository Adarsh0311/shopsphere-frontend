export interface CartItemResponse {
  // Define the properties for cart items based on your CartItemResponse class
  // Example fields (update according to your actual model):
  id: string;
  productId: string;
  productName: string;
  quantity: number;
  price: number;
  itemTotal: number;
}